{
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "vendor-dir": "vendor-test"
    },
    "require": {
        "php": ">=7.2",
        "symfony/filesystem": "*"
    }
}